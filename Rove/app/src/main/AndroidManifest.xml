<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.gursimransinghhanspal.rove">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Rove.Base">

        <activity
            android:name=".activity.Splash"
            android:theme="@style/Rove.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".controller.DeeplinkController">
        </activity>

        <activity
            android:name=".activity.LoginViaFacebook"
            android:theme="@style/Rove.NoActionBarSolidBg">
        </activity>
    </application>

</manifest>
